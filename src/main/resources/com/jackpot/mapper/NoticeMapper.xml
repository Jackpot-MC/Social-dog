<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jackpot.mapper.NoticeMapper">

	<resultMap id="noticeMap" type="com.jackpot.domain.NoticeVO">
		<id property="noticeId" column="notice_id" />
		<result property="noticeTitle" column="notice_title" />
		<result property="noticeContent" column="notice_content" />
		<result property="adminId" column="admin_id" />
	</resultMap>
	
	<select id = "read" resultMap="noticeMap">
		select notice_id, notice_title, notice_content, admin_id
		from notice
		where notice_id = #{noticeId}
	</select>
	
	<insert id = "insert">
		insert into notice(notice_title, notice_content, admin_id)
		values(#{noticeTitle}, #{noticeContent}, #{adminId})

		<selectKey resultType= "Long" keyProperty="noticeId" keyColumn="notice_id" order="AFTER">
			SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>
	
	<update id = "update">
		update notice set
			notice_title = #{noticeTitle},
			notice_content = #{noticeContent}
		where notice_id = #{noticeId}
	</update>
	
	<delete id="delete">
		delete from notice where notice_id = #{noticeId}
	</delete>

</mapper>