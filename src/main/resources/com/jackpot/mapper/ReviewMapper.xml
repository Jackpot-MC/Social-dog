<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jackpot.mapper.ReviewMapper">

	<resultMap id="reviewMap" type="com.jackpot.domain.ReviewVO">
		<id property="reviewId" column="review_id" />
		<result property="memberId" column="member_id" />
		<result property="placeId" column="place_id" />
		<result property="reviewTitle" column="review_title" />
		<result property="reviewContent" column="review_content" />
		<result property="rating" column="rating" />
		<result property="memberName" column="member_name" />
		<result property="regDate" column="reg_date" />
		<result property="updateDate" column="update_date" />
	</resultMap>
	
	<select id = "read" resultMap="reviewMap">
		select review_id, review.member_id, place_id, review_title, review_content, rating, m.member_name, reg_date, update_date
		from review left outer join `member` m 
		on review.member_id = m.member_id
		where review_id = #{reviewId} 
	</select>
	
	<insert id = "insert">
		insert into review (member_id, place_id, review_title, review_content, rating)
		values (#{memberId}, #{placeId}, #{reviewTitle}, #{reviewContent}, #{rating})

		<selectKey resultType= "Long" keyProperty="reviewId" keyColumn="review_id" order="AFTER">
			SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>
	
	<update id = "update">
		update review
		set review_title = #{reviewTitle}, 
			review_content = #{reviewContent}, 
			rating = #{rating}
		where review_id = #{reviewId}
	</update>
	
	<delete id="delete">
		delete from review
		where review_id = #{reviewId}
	</delete>
	
	<select id="getList" resultType="com.jackpot.domain.ReviewVO">
		select review_id, member_id, place_id, review_title, review_content, rating, reg_date, update_date
		from review
	</select>

	<select id="getListWithPaging" resultType="ReviewVO">
		select review_id, review.member_id, place_id, review_title, review_content, rating, m.member_name, reg_date, update_date
		from review left outer join `member` m 
		on review.member_id = m.member_id
		<where>
			<include refid="criteria"></include>
		</where>
		order by review_id
		limit #{offset}, #{amount}
	</select>
	
	<select id="getTotalCount" resultType="int">
		select count(*) from review
		<where>
			<include refid="criteria"></include>
		</where>
	</select>
	
	<select id="getAverage" resultType="int">
		SELECT CONVERT(AVG(rating), SIGNED INTEGER) AS 'average' FROM review
	</select>
	
	<sql id="criteria"> <!-- 중복 SQL 구문 분리, id 지정 -->
		<trim prefixOverrides = "OR">
			<foreach item = "type" collection="typeArr">
				<trim prefix="OR">
					<choose>
						<when test="type == 'T'.toString()">
							review_title like CONCAT('%',#{keyword},'%')
						</when>
						<when test="type == 'C'.toString()">
							review_content like CONCAT('%',#{keyword},'%')
						</when>
						<when test="type == 'A'.toString()">
							review.member_id like CONCAT('%',#{keyword},'%')
						</when>
					</choose>
				</trim>
			</foreach>
		</trim>	
	</sql>
	
</mapper>